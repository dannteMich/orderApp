import React, {useContext} from 'react';

interface Props {
    accountId: string;
}

const OrderBuilderContainer: React.FC<Props> = ({accountId}) => {
    return <div>Order builder container</div>
}

export default OrderBuilderContainer;